apiVersion: app/v1
kine: Service
type: ClusterIP
metadata:
	name: aggregate-article-service
spec:
	selector:
		matchLabels:
      app: aggregate-article-pod
		ports:
			- port: 80
			  targetPort: 1024